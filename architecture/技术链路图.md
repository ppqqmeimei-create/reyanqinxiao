# 热眼擒枭 - 系统整体技术链路图

## 感知层 → 边缘计算层 → 数据融合层 → 存储层 → 应用层 → 执法闭环

---

### 一、整体链路架构图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '12px',
    'nodeSpacing': '45',
    'rankSpacing': '65',
    'primaryColor': '#1976d2',
    'edgeLabelBackground': '#ffffff',
    'nodeBorder': '#d9d9d9',
    'mainBkg': '#ffffff',
    'lineColor': '#333333'
}}}%%
flowchart TD
    %% ============ 主标题（仅层级锁定，箭头隐藏） ============
    TITLE["<b>🔥 热眼擒枭 - 系统整体技术链路图</b>"]
    SUBTITLE["<i>边境活物走私智能防控全链路闭环系统</i>"]

    %% ============ 1. 感知层（和参考图节点顺序/内容1:1还原） ============
    subgraph SENSING["<b>🛰️ 感知层（边境传感器网络）</b>"]
        direction LR
        S1["🚁 无人机巡检系统\n广西总部调度"]
        S2["〰️ 边境震动光纤\n那坡界碑 / 靖西岳圩"]
        S3["📡 活体探测雷达\n凭祥 / 龙州水口"]
        S4["📹 卡口抓拍摄像头\n万尾金滩 / 友谊关"]
        S5["📷 红外热成像摄像头\n友谊关 / 东兴 / 龙州 / 那坡"]
    end

    %% ============ 2. 边缘计算层（和参考图节点顺序/内容1:1还原） ============
    subgraph EDGE["<b>⚡ 边缘计算层（边缘节点）</b>"]
        direction LR
        E1["EDGE-GX-HQ\n广西总部指挥节点"]
        E2["EDGE-NAPO-01\n那坡节点（百色）"]
        E3["EDGE-LONGZHOU-01\n龙州节点（崇左）"]
        E4["EDGE-PINGXIANG-01\n凭祥节点（崇左）"]
        E5["EDGE-DONGXING-01\n东兴节点（防城港）"]
    end

    %% ============ 3. 数据融合层（和参考图垂直串行布局1:1还原） ============
    subgraph FUSION["<b>🧠 数据融合层（Node.js / Express）</b>"]
        direction TB
        F1["多传感器时空融合引擎\n坐标对齐·时间戳同步·冲突消解"]
        F2["AI物种识别模块\nCITES等级判定·置信度评分"]
        F3["走私风险评分引擎\n0-100分·历史案件加权"]
        F4["预警生成器\nSSE 实时推送"]
    end

    %% ============ 4. 存储层（MySQL圆柱样式和参考图1:1还原） ============
    subgraph STORAGE["<b>🗄️ 存储层</b>"]
        direction LR
        DB2["本地离线缓存\n广西边境离线地图包"]
        DB1[("MySQL\nalerts / locations\ndevices / tasks")]
    end

    %% ============ 5. 应用层（和参考图节点顺序/内容1:1还原） ============
    subgraph APP["<b>📱 应用层（Vue3 + uni-app）</b>"]
        direction LR
        A1["🗺️ GIS态势一张图\n五图层叠加显示"]
        A2["📊 指挥大屏\nECharts 走私时段分析"]
        A3["✅ 任务执行页\n执法检查清单·取证水印"]
        A4["🔧 传感器网络页\n设备健康度监控"]
        A5["🔴 预警工作台\n实时走私线索流"]
    end

    %% ============ 核心流向控制 ============
    TITLE --> SUBTITLE
    SUBTITLE -.-> SENSING

    %% 1. 感知层 → 边缘计算层
    S1 --> E1
    S2 --> E1
    S2 --> E2
    S3 --> E3
    S3 --> E4
    S4 --> E4
    S4 --> E5
    S5 --> E1
    S5 --> E2
    S5 --> E3
    S5 --> E4
    S5 --> E5

    %% 2. 边缘计算层 → 数据融合层
    E1 --> F1
    E2 --> F1
    E3 --> F1
    E4 --> F1
    E5 --> F1

    %% 3. 数据融合层内部串行
    F1 --> F2
    F2 --> F3
    F3 --> F4

    %% 4. 预警闭环：预警生成器 → 边缘节点
    F4 --> E1
    F4 --> E2
    F4 --> E3
    F4 --> E4
    F4 --> E5

    %% 5. 全链路数据落库
    E1 --> DB1
    E2 --> DB1
    E3 --> DB1
    E4 --> DB1
    E5 --> DB1
    F1 --> DB1
    F4 --> DB1

    %% 6. 存储层 → 应用层
    DB1 --> A1
    DB1 --> A2
    DB1 --> A3
    DB1 --> A4
    DB1 --> A5
    DB2 --> A1

    %% ============ 样式定义 ============
    classDef SENSING fill:#f0f7ff,stroke:#4096ff,stroke-width:2px,color:#0050b3
    classDef EDGE fill:#fff5e6,stroke:#ff9a2e,stroke-width:2px,color:#d46b08
    classDef FUSION fill:#f2f9f2,stroke:#52c41a,stroke-width:2px,color:#237804
    classDef STORAGE fill:#f8f5ff,stroke:#9254de,stroke-width:2px,color:#531dab
    classDef APP fill:#fff5f5,stroke:#ff4d4f,stroke-width:2px,color:#a8071a
    classDef baseNode rx:4px,stroke:#d9d9d9,stroke-width:1px,fill:#ffffff
    classDef dbNode rx:8px,stroke:#d9d9d9,stroke-width:1px,fill:#ffffff

    class S1,S2,S3,S4,S5 baseNode
    class E1,E2,E3,E4,E5 baseNode
    class F1,F2,F3,F4 baseNode
    class DB2 baseNode
    class DB1 dbNode
    class A1,A2,A3,A4,A5 baseNode
```

---

### 二、技术栈全景图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '11px',
    'nodeSpacing': '15',
    'rankSpacing': '30'
}}}%%
flowchart TB
    subgraph FRONTEND["<b>📱 前端技术栈</b>"]
        F1["uni-app 跨端框架"]
        F2["Vue 3 Composition API"]
        F3["Pinia 状态管理"]
        F4["SCSS 样式预处理器"]
        F5["WebSocket 实时通信"]
        F6["Mapbox GL 地图引擎"]
        F7["ECharts 数据可视化"]
    end

    subgraph BACKEND["<b>⚙️ 后端技术栈</b>"]
        B1["Node.js 运行时"]
        B2["Express.js Web框架"]
        B3["Sequelize ORM"]
        B4["WebSocket 实时推送"]
        B5["JWT 身份认证"]
        B6["Joi 数据验证"]
        B7["Winston 日志系统"]
    end

    subgraph DATABASE["<b>🗄️ 数据库技术</b>"]
        D1["MySQL 8.0 关系型数据库"]
        D2["Redis 缓存数据库"]
        D3["SQLite 离线缓存"]
        D4["Helmet 安全中间件"]
        D5["bcryptjs 密码加密"]
    end

    subgraph AI["<b>🤖 AI能力层</b>"]
        A1["YOLO 目标检测"]
        A2["物种识别模型"]
        A3["行为分析引擎"]
        A4["风险评分算法"]
    end

    FRONTEND --> BACKEND
    BACKEND --> DATABASE
    AI --> BACKEND

    classDef FRONTEND fill:#bbdefb,stroke:#1565c0,color:#0d47a1
    classDef BACKEND fill:#e1bee7,stroke:#7b1fa2,color:#4a148c
    classDef DATABASE fill:#c8e6c9,stroke:#388e3c,color:#1b5e20
    classDef AI fill:#ffe0b2,stroke:#f57c00,color:#e65100

    class FRONTEND FRONTEND
    class BACKEND BACKEND
    class DATABASE DATABASE
    class AI AI
```

---

### 三、数据流向链路图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '11px'
}}}%%
flowchart LR
    subgraph INPUT["数据输入"]
        I1["传感器数据\n(MQTT)"]
        I2["图像/视频流\n(RTSP)"]
        I3["人工上报\n(REST API)"]
        I4["GPS轨迹\n(WebSocket)"]
    end

    subgraph EDGE_PROC["边缘计算处理"]
        E1["数据清洗"]
        E2["特征提取"]
        E3["本地AI推理"]
        E4["异常检测"]
    end

    subgraph CLOUD_PROC["云端融合处理"]
        C1["多源数据融合"]
        C2["时空对齐"]
        C3["AI二次识别"]
        C4["风险评分"]
    end

    subgraph OUTPUT["业务输出"]
        O1["实时预警推送"]
        O2["任务自动派发"]
        O3["GIS可视化"]
        O4["数据统计分析"]
    end

    INPUT --> EDGE_PROC
    EDGE_PROC --> CLOUD_PROC
    CLOUD_PROC --> OUTPUT

    classDef INPUT fill:#e3f2fd,stroke:#1976d2
    classDef EDGE_PROC fill:#f3e5f5,stroke:#7b1fa2
    classDef CLOUD_PROC fill:#e8f5e9,stroke:#388e3c
    classDef OUTPUT fill:#fce4ec,stroke:#c2185b

    class INPUT INPUT
    class EDGE_PROC EDGE_PROC
    class CLOUD_PROC CLOUD_PROC
    class OUTPUT OUTPUT
```

---

### 四、核心数据模型关系图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '10px'
}}}%%
erDiagram
    USER ||--o{ ALERT : "assigned_to"
    USER ||--o{ ALERT : "created_by"
    USER ||--o{ ALERT : "resolved_by"
    USER ||--o{ TASK : "assigned_to"
    USER ||--o{ TASK : "created_by"
    USER ||--o{ DEVICE : "responsible_officer"
    
    ALERT ||--o{ TASK : "triggers"
    ALERT ||--o| WILDLIFE_CASE : "escalates_to"
    
    TASK ||--o{ TASK_EVIDENCE : "contains"
    TASK ||--o{ TASK_CHECKLIST : "includes"
    TASK ||--o{ TASK_COLLABORATION : "involves"
    
    DEVICE ||--o{ DEVICE_METADATA : "generates"
    DEVICE ||--o{ SMUGGLING_ALERT : "triggers"
    
    WILDLIFE_CASE ||--o{ BIOLOGICAL_SAMPLE : "includes"
    
    USER {
        int id PK
        string badge_number UK
        string username
        string email
        string password_hash
        enum role
        bool is_active
        datetime last_login
    }
    
    ALERT {
        int id PK
        enum level
        enum type
        enum status
        float risk_score
        float latitude
        float longitude
        string border_section
        int assigned_to FK
    }
    
    TASK {
        int id PK
        string title
        enum status
        int alert_id FK
        int assigned_to FK
        float progress
        string joint_case_no
    }
    
    DEVICE {
        int id PK
        string device_id UK
        enum type
        enum status
        float latitude
        float longitude
        datetime last_heartbeat
    }
```

---

### 五、业务流程时序图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '10px'
}}}%%
sequenceDiagram
    participant S as 传感器
    participant E as 边缘节点
    participant F as 融合引擎
    participant A as 预警系统
    participant M as MySQL
    participant UI as 用户界面
    participant U as 执法人员

    S->>E: MQTT实时数据
    E->>E: 本地AI推理
    E->>F: 边缘预处理结果
    
    F->>F: 时空数据融合
    F->>F: AI物种识别
    F->>F: 风险评分计算
    
    F->>A: 生成预警
    A->>M: 存储预警数据
    M-->>A: 确认存储
    
    A->>UI: SSE实时推送
    UI-->>U: 显示预警卡片
    
    U->>UI: 查看预警详情
    UI->>M: 获取预警信息
    M-->>UI: 返回数据
    
    U->>UI: 派发执法任务
    UI->>M: 创建任务记录
    M-->>UI: 任务创建成功
    
    U->>UI: 执行取证操作
    UI->>M: 上传证据文件
    M-->>UI: 证据哈希存证
    
    U->>UI: 完成任务
    UI->>M: 更新任务状态
    M-->>UI: 任务归档
```

---

### 六、API接口链路图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
    'fontSize': '10px'
}}}%%
flowchart TB
    subgraph API["API接口 (/api/v1)"]
        subgraph AUTH["认证模块"]
            A1["POST /auth/login"]
            A2["POST /auth/biometric"]
            A3["POST /auth/logout"]
        end
        
        subgraph ALERT["预警模块"]
            B1["GET /alerts"]
            B2["GET /alerts/:id"]
            B3["POST /alerts"]
            B4["PUT /alerts/:id"]
            B5["PUT /alerts/:id/assign"]
        end
        
        subgraph TASK["任务模块"]
            C1["GET /tasks"]
            C2["POST /tasks"]
            C3["PUT /tasks/:id"]
            C4["POST /tasks/:id/complete"]
        end
        
        subgraph DEVICE["设备模块"]
            D1["GET /devices"]
            D2["GET /devices/:id/status"]
            D3["PUT /devices/:id/heartbeat"]
        end
        
        subgraph FUSION["融合模块"]
            E1["GET /fusion/events"]
            E2["GET /fusion/metrics"]
            E3["POST /fusion/dispatch"]
        end
        
        subgraph WS["实时通信"]
            W1["WS /ws/command"]
            W2["SSE /sse/alerts"]
        end
    end

    classDef AUTH fill:#ffcdd2,stroke:#d32f2f
    classDef ALERT fill:#f8bbd0,stroke:#c2185b
    classDef TASK fill:#e1bee7,stroke:#7b1fa2
    classDef DEVICE fill:#d1c4e9,stroke:#512da8
    classDef FUSION fill:#c5cae9,stroke:#303f9f
    classDef WS fill:#bbdefb,stroke:#1976d2

    class AUTH AUTH
    class ALERT ALERT
    class TASK TASK
    class DEVICE DEVICE
    class FUSION FUSION
    class WS WS
```

---

### 七、技术特性总览表

| 层级 | 技术特性 | 实现方案 |
|:-----|:---------|:---------|
| **感知层** | 多源数据采集 | MQTT/RTSP/HTTP多协议支持 |
| | 边缘AI推理 | TensorRT/NCNN本地部署 |
| | 低延迟响应 | 边缘节点就近处理 |
| **融合层** | 时空数据融合 | 坐标对齐+时间戳同步 |
| | AI物种识别 | YOLO+专项分类模型 |
| | 风险量化评估 | 多维度加权评分算法 |
| **存储层** | 高并发读写 | MySQL主从+Redis缓存 |
| | 离线数据保障 | SQLite本地缓存+CRDT同步 |
| | 证据存证 | SHA-256哈希+时间戳 |
| **应用层** | 跨端统一体验 | uni-app一套代码多端运行 |
| | 实时数据推送 | SSE+WebSocket双通道 |
| | 离线优先 | 离线队列+智能同步 |
| **安全层** | 身份认证 | JWT+生物识别 |
| | 数据传输 | HTTPS+Token签名 |
| | 操作审计 | 完整日志+操作追溯 |

---

**热眼擒枭 - 用科技守护绿水青山，用智慧筑牢边境防线**
